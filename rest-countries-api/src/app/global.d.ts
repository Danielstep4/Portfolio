export interface Theme {
  pallete: {
    dark: {
      elements: string;
      background: string;
      text: string;
    };
    light: {
      elemetns: string;
      background: string;
      text: string;
      input: string;
    };
  };
}
